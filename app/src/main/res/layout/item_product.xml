<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/txProduct"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/ivToDetail"
        app:layout_constraintBottom_toBottomOf="parent"
        android:maxLines="1"
        android:layout_marginHorizontal="8dp"
        android:layout_marginVertical="12dp"
        android:ellipsize="end"
        android:gravity="start"
        android:textSize="@dimen/product_list_name"
        android:textStyle="bold"
        tools:text="@string/lore_ipsum"/>
    
    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/ivToDetail"
        android:layout_width="@dimen/next_arrow"
        android:layout_height="@dimen/next_arrow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/ic_next_arrow"
        android:tint="?attr/colorPrimary"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
    
</layout>